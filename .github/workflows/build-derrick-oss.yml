---
name: build_derrick

on:
  workflow_call:
    inputs:
      bundle-path:
        required: false
        type: string
      cgo-enabled:
        type: string
        default: 0
      create-packages:
        type: boolean
        default: true
      goos:
        required: true
        type: string
      goarch:
        required: true
        type: string
      go-tags:
        type: string
      go-version:
        type: string
      package-name:
        type: string
        required: true
      derrick-version:
        type: string
        required: true
      derrick-base-version:
        type: string
        required: true
      derrick-prerelease:
        type: string
        required: true

jobs:
  build:
    runs-on: ubuntu-latest
    outputs:
      bin-name: ${{ steps.get-bin-name.outputs.bin-name }}
    name: Build ${{ inputs.package-name }} ${{ inputs.goos }} ${{ inputs.goarch }} v${{ inputs.derrick-version }}
    steps:
      - uses: actions/checkout@8e5e7e5ab8b370d6c329ec480221332ada57f0ab # v3.5.2
      - name: Restore ui assets
        uses: actions/cache/restore@v4
        with:
          path: ui/dist/assets
          key: ui_assets_${{ github.sha }}
          fail-on-cache-miss: true
          enableCrossOsArchive: true
      - name: Restore build assets
        uses: actions/cache/restore@v4
        with:
          path: internal/assets
          key: build_assets_${{ github.sha }}
          fail-on-cache-miss: true
          enableCrossOsArchive: true
      - name: Determine binary name
        id: get-bin-name
        run: |
          bin_name="${{ inputs.package-name }}"
          if [ "${{ inputs.goos }}" == "windows" ];then
               bin_name="${bin_name}.exe"
          fi
          echo "bin-name=${bin_name}" | tee -a "${GITHUB_OUTPUT}"
      - name: Build binary
        env:
          GOOS: ${{ inputs.goos }}
          GOARCH: ${{ inputs.goarch }}
          DERRICK_GOOS: ${{ inputs.goos }}
          DERRICK_GOARCH: ${{ inputs.goarch }}
          BASE_VERSION: ${{ inputs.derrick-base-version }}
          PRERELEASE: ${{ inputs.derrick-prerelease }}
          CGO_ENABLED: ${{ inputs.cgo-enabled }}
          CRT_BIN_NAME: ${{ steps.get-bin-name.outputs.bin-name }}
        uses: hashicorp/actions-go-build@v0.1.7
        with:
          product_name: ${{ inputs.package-name }}
          product_version: ${{ inputs.derrick-version }}
          go_version: ${{ inputs.go-version }}
          os: ${{ inputs.goos }}
          arch: ${{ inputs.goarch }}
          reproducible: nope
          instructions: |
            make bin/crt-${{ inputs.package-name }}

      - name: Linux packaging
        id: packaging
        if: ${{ (inputs.goos == 'linux') && (inputs.package-name == 'derrick') && (inputs.goarch != '386') }}
        uses: hashicorp/actions-packaging-linux@v1
        with:
          name: ${{ inputs.package-name }}
          description: ${{ inputs.package-name }}
          arch: ${{ inputs.goarch }}
          version: ${{ inputs.derrick-version }}
          maintainer: "Nomatron"
          homepage: "https://github.com/nomatronio/derrick"
          license: "MPL-2.0"
          binary: "dist/${{ inputs.package-name }}"
          deb_depends: "git"
          rpm_depends: "git"
      - name: Set Package Names
        if: steps.packaging.conclusion == 'success'
        run: |
          echo "RPM_PACKAGE=$(basename out/*.rpm)" >> "$GITHUB_ENV"
          echo "DEB_PACKAGE=$(basename out/*.deb)" >> "$GITHUB_ENV"
      - name: Upload rpm package
        uses: actions/upload-artifact@v4
        if: steps.packaging.conclusion == 'success'
        with:
          name: ${{ env.RPM_PACKAGE }}
          path: out/${{ env.RPM_PACKAGE }}
      - name: Upload deb package
        uses: actions/upload-artifact@v4
        if: steps.packaging.conclusion == 'success'
        with:
          name: ${{ env.DEB_PACKAGE }}
          path: out/${{ env.DEB_PACKAGE }}
