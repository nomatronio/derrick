---
layout: commands
page_title: 'Commands: Runner uninstall'
sidebar_title: 'runner uninstall'
description: 'Uninstall a Derrick runner from Kubernetes, Nomad, ECS, or Docker'
---

# Derrick Runner uninstall

Command: `derrick runner uninstall`

Uninstall a Derrick runner from Kubernetes, Nomad, ECS, or Docker

@include "commands/runner-uninstall_desc.mdx"

## Usage

Usage: `derrick runner uninstall [options]`

Uninstall a Derrick runner from server given a platform and Derrick runner
id. The platform should be specified as kubernetes, nomad, ecs, or docker.

This will forget the runner on the server and remove any of the resources
created by a runner installation.

#### Global Options

- `-plain` - Plain output: no colors, no animation. The default is false.
- `-app=<string>` (`-a`) - App to target. Certain commands require a single app target for Derrick configurations with multiple apps. If you have a single app, then this can be ignored.
- `-project=<string>` (`-p`) - Project to target.
- `-workspace=<string>` (`-w`) - Workspace to operate in.

#### Command Options

- `-platform=<string>` - Platform to uninstall the Derrick runner from.
- `-server-addr=<string>` - Address of the Derrick server.
- `-id=<string>` - ID of the Derrick runner.

#### ecs Options

- `-ecs-region=<string>` - AWS region in which to install the Derrick runner.
- `-ecs-cluster=<string>` - The name of the ECS Cluster to install the Derrick runner into. The default is derrick-server.

#### kubernetes Options

- `-k8s-config-path=<string>` - Path to the kubeconfig file to use.
- `-k8s-context=<string>` - The Kubernetes context to install the Derrick runner to. If left unset, Derrick will use the current Kubernetes context.
- `-k8s-namespace=<string>` - The namespace in the Kubernetes cluster into which the Derrick runner will be installed. The default is default.

@include "commands/runner-uninstall_more.mdx"
